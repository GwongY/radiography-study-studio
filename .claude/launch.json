{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "osteology-studio",
      "autoPort": true,
      "runtimeExecutable": "python",
      "runtimeArgs": [
        "-c",
        "import os,functools,http.server;port=int(os.environ.get('PORT','8420'));Handler=functools.partial(http.server.SimpleHTTPRequestHandler,directory='outputs');http.server.ThreadingHTTPServer.allow_reuse_address=True;http.server.ThreadingHTTPServer.daemon_threads=True;http.server.ThreadingHTTPServer(('127.0.0.1',port),Handler).serve_forever()"
      ],
      "port": 8420
    }
  ]
}
