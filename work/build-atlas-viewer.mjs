// Reproducible adaptation of ashemag/human-atlas (MIT). Sources preserved in atlas-source.
// Node 24 strips TypeScript; the shipped app remains plain, no-build ES modules.
import {readFileSync,writeFileSync} from 'node:fs';
import {stripTypeScriptTypes} from 'node:module';
const read=name=>readFileSync(`work/atlas-source/${name}.ts`,'utf8');
const write=(name,s)=>writeFileSync(`outputs/atlas/${name}.js`,'// Adapted from ashemag/human-atlas. MIT; see ../THIRD-PARTY-NOTICES.txt.\n'+stripTypeScriptTypes(s).replace(/[ \t]+$/gm,''));
for(const name of ['explosion-layout','model-download','pointer-tap'])write(name,read(name));
write('anatomy',read('anatomy').split('export const EXPLANATIONS')[0].replace(/description:'[^']*'/g,"description:''"));
let s=read('scene');
s=s.replace("import {useEffect,useRef} from 'react';\n",'');
const start=s.indexOf('interface Props'),end=s.indexOf('let disposed=false',start);
s=s.slice(0,start)+'export function createAtlasScene(el:HTMLElement,atlas:Atlas,state:SceneState,onSelect:(id:string)=>void,onProgress:(n:number)=>void,onError:(s:string)=>void){\n const latest={current:state},select={current:onSelect};let active=true;\n '+s.slice(end);
s=s.replaceAll("'three/examples/jsm/","'https://cdn.jsdelivr.net/npm/three@0.161.0/examples/jsm/");
s=s.replace('const materials:T.Material[]=[]','const batches:{mesh:T.Mesh;system:string}[]=[];const materials:T.Material[]=[]');
s=s.replace('mesh.frustumCulled=false;scene.add(mesh);','mesh.frustumCulled=false;scene.add(mesh);batches.push({mesh,system});');
s=s.replace('partTexture.needsUpdate=true;selectionTexture.needsUpdate=true;',"for(const batch of batches)batch.mesh.visible=atlas.parts.some(p=>p.system===batch.system&&(s.isolate?selection.has(p.id):visible.has(p.system)||selection.has(p.id)));partTexture.needsUpdate=true;selectionTexture.needsUpdate=true;");
s=s.replace('Math.min(clock.getDelta(),.05)','Math.min(clock.getDelta(),.2)');
s=s.replace(/renderer.setPixelRatio\([^;]+\);/g,'renderer.setPixelRatio(Math.min(devicePixelRatio,1.5,1600/Math.max(1,el.clientWidth)));');
for(const name of ['explosion-layout','model-download','pointer-tap','anatomy'])s=s.replaceAll(`'./${name}'`,`'./${name}.js'`);
s=s.replace("const chunk=atlas.chunks[ci],compressed=!!chunk.gzip&&typeof DecompressionStream!=='undefined';const response=await fetch(compressed?chunk.gzip!:chunk.url,{signal:abort.signal});","const chunk=atlas.chunks[ci],compressed=true;if(typeof DecompressionStream==='undefined')throw new Error('This browser cannot decompress the full atlas. Use an updated browser.');const response=await fetch(new URL('./models/'+chunk.gzip!.split('/').pop(),import.meta.url),{signal:abort.signal});");
const fitStart=s.indexOf('   const aspect=camera.aspect'),fitEnd=s.indexOf('\n  };',fitStart);
s=s.slice(0,fitStart)+`   const normalDistance=2/Math.min(1,camera.aspect)/(2*Math.tan(T.MathUtils.degToRad(camera.fov/2)))*1.15;
   const inventoryDistance=Math.max(packingHeight,packingWidth/camera.aspect)/(2*Math.tan(T.MathUtils.degToRad(camera.fov/2)))*1.15;
   const distance=T.MathUtils.lerp(normalDistance,Math.max(.2,inventoryDistance),extent);
   const direction=extent>.8||view==='front'?new T.Vector3(0,0,1):new T.Vector3(.35,.06,1).normalize();
   controls.target.set(0,.85,0);camera.position.copy(controls.target).addScaledVector(direction,distance);controls.update();dirty=true;`+s.slice(fitEnd);
s=s.replace("if(disposed)return;frame=requestAnimationFrame(animate);const dt=", "if(disposed)return;frame=requestAnimationFrame(animate);if(!active||!el.clientWidth||!el.clientHeight)return;const dt=");
const isoStart=s.indexOf('const w=el.clientWidth,h=el.clientHeight'),isoEnd=s.indexOf('controls.maxDistance=Math.max(40,distance*2)',isoStart);
s=s.slice(0,isoStart)+"const distance=Math.max(.07,Math.max(size.y,size.x/camera.aspect,size.z)/(2*Math.tan(T.MathUtils.degToRad(camera.fov/2)))*1.25);"+s.slice(isoEnd);
s=s.replace('  return()=>{disposed=true;','  const dispose=()=>{disposed=true;');
const tail=s.indexOf(' },[atlas]);');
s=s.slice(0,tail)+` return {dispose,update(next:SceneState){latest.current=next;},setActive(value:boolean){active=value;if(value){resize();dirty=true;}},inspect(){return {ready,active,renderedFrames:renderer.info.render.frame,target:targets[0],amount,parts:pickers.filter(Boolean).length,drawCalls:renderer.info.render.calls,triangles:renderer.info.render.triangles,packingWidth,packingHeight};}};\n}\n`;
write('scene',s);
