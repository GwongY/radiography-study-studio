# Radiography Study Studio — working guide

This file is a pointer, not a copy.

**The working guide is [`CLAUDE.md`](CLAUDE.md). Read that.**

It was duplicated here once, and the copy silently rotted two commits behind the
original — wrong part counts, and missing the two checks that guard four years of
progress data. An agent reading the stale half would have skipped them. Every
other fact in this repo is either generated or checked; a hand-kept second copy of
the one file every session loads is neither.

So: one guide, one file. If you are an agent that looks for `AGENTS.md`, you have
found it — now open `CLAUDE.md`.
